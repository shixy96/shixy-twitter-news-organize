# Pipeline Run Issues — 2026-04-03

> 来源：Haiku subagent 首次完整 pipeline 运行（run-2026-04-03-112526Z）观察总结。

---

## A. 执行流程问题（来自 run.log.jsonl）

### A1. x_list_fetch 重复执行 5 次 🔴
- 第 1 次写出的 raw.json 在 char 128941 处截断（JSON 不完整）
- raw_validate 失败后，subagent 不知道如何恢复，反复重跑 fetch
- 浪费约 80 秒和 4 次 Twitter API 调用
- 根因：fetch 脚本不使用原子写入（tmp → rename），也无幂等保护；SKILL.md 未说明 validate 失败的恢复策略

### A2. enrich.py 重复执行两次，render_md.py 竞态 🔴
- 第一次 enrich 运行到 item 8 被中断（11:30:40），随即 render_md 抢先在 11:34:35 启动读取不完整 post.json
- 第二次 enrich 从头开始跑完全部 10 条，render_md 再跑一次
- 前 8 条 tweet 被处理了两遍，render_md 读到过时数据

### A3. run_id 中途丢失 🟠
- 第二次 enrich 及之后（TTS、render_md）所有日志的 `run_id` 为空字符串
- 日志事件无法关联到同一次 run，可观测性中断

### A4. output_path 字段未记录 🟢
- fetch 和 filter 的 `step_complete` 日志中 `"output_path": ""`
- 无法从日志确认产物实际写入路径

### A5. fetch_log 中途变 null 🟢
- 第 3-5 次 fetch 日志中 `"fetch_log": null`，subagent 传参不一致

---

## B. Step 3 Agent 组装步骤被完全跳过 🔴（最核心问题）

SKILL.md 定义的工作流：enrich → **Agent 组装** → render_md → validate_output

subagent 跳过了 Step 2（Agent 组装）和 Step 4（validate_output.py），直接 enrich → render_md。

### B1. title 未翻译/重写
- post.json 所有 item 的 `title` 是原始英文推文截断到 80 字符
- 应由 Agent 基于 companion + enrichment 生成中文标题

### B2. body 未翻译/改写
- 所有 `body` 是原始英文推文 + "该内容获得…点赞…收藏" 机械拼接
- 应由 Agent 生成信息完整、语言流畅的中文段落（高价值条目 3-4 段，普通条目 3+ 句）

### B3. related_links 全为空
- enrich.py 的 `link_fetches` 已收集好，但无 Agent 步骤将其映射到 post.json
- 所有条目 `"related_links": []`

### B4. media 全为空
- enrich.py 的 `media_files` 已下载（is_highlight 条目），但同样无 Agent 映射
- 所有条目 `"media": []`

### B5. validate_output.py 从未运行
- 应校验 title 非直拷、item 数一致、highlight 覆盖率等，实际未执行

---

## C. 内容质量问题

### C1. 非 AI 内容混入（3 条）🔴
- Trump 医疗言论 → 分类"行业观点"（31k 点赞触发 engagement 筛选通过）
- Trump 攻击 Springsteen → 分类"开发生态"（27k 点赞）
- Wikipedia 字节删减趣闻 → 分类"行业观点"
- 根因：filter.py 只按 engagement 阈值过滤；editorial step LLM 未拒绝非 AI 内容

### C2. companion.json schema 错误（QA: 2 errors）🟠
- `root.items[6].strong_links[0]`：expected string, got dict
- `root.items[6].external_links[0]`：expected string, got dict
- enrich.py 可容错，但 schema 定义与实际产出不一致

### C3. editorial_review 6 条警告 🟢
- items[3][8][10] body 首段过短（<45 字符）
- items[7][8] 某段落过长（>220 字符）
- "模型发布"分类占比 70%（>60% 阈值）

---

## D. 依赖环境问题

### D1. edge_tts Python 模块未安装 🟢
- TTS 降级到 CLI，功能正常但留下 warning 日志
- 需在环境文档中明确 Python 依赖

---

## 优先级汇总

| 优先级 | ID | 问题 | 影响范围 |
|--------|-----|------|---------|
| 🔴 P0 | B1-B4 | Agent 组装步骤被跳过 | 产物核心质量 |
| 🔴 P0 | C1 | 非 AI 内容混入 | 产物核心质量 |
| 🔴 P1 | A1 | fetch 重复执行 5 次 | 运行稳定性/成本 |
| 🔴 P1 | A2 | enrich 重复执行 + render_md 竞态 | 运行稳定性 |
| 🟡 P1 | B5 | validate_output.py 未运行 | 质量门禁缺失 |
| 🟠 P2 | A3 | run_id 中途丢失 | 可观测性 |
| 🟠 P2 | C2 | companion schema 错误 | 数据一致性 |
| 🟢 P3 | A4/A5 | 日志字段不完整 | 可观测性 |
| 🟢 P3 | D1 | edge_tts 模块缺失 | 环境依赖文档 |
